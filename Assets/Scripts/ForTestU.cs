using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ForTestU : MonoBehaviour
{
    public Task taskname;

    // Start is called before the first frame update
    void Start()
    {
        taskname.StartTask();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
